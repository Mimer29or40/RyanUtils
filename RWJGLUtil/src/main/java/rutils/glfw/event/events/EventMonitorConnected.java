package rutils.glfw.event.events;

import rutils.glfw.Monitor;

public class EventMonitorConnected extends EventMonitor
{
    public EventMonitorConnected(Monitor monitor)
    {
        super(monitor);
    }
}
